const fontSize = 10;

const paneTitle = {
  fontSize: 12,
  fontWeight: 'bold',
};

const paneLabel = {
  fontSize: 10,
  fontWeight: 'normal',
};

const paragraph = {
  fontSize: fontSize,
};

const subscript = {
  fontSize: (fontSize * 3 / 4),
  lineHeight: 10,
  textAlignVertical: 'bottom',
};

export default {
  fontSize,
  paneLabel,
  paneTitle,
  paragraph,
  subscript,
};
